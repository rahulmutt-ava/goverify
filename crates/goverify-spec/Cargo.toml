[package]
name = "goverify-spec"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
goverify-ir.workspace = true
goverify-solver.workspace = true
goverify-analysis.workspace = true

# `declared_name_wins_over_synthetic_ret_alias` builds a hand-written temp
# module (a name-collision shape no existing corpus fixture has) via
# `goverify_ir::testutil::load_module`.
[dev-dependencies]
tempfile.workspace = true
